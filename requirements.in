# Direct dependencies for research, training, validation, and demos.
# Compile after reviewing upgrades:
#   pip-compile --strip-extras --output-file=requirements.txt requirements.in
numpy>=1.24,<3
pandas>=2.0,<3
scikit-learn>=1.3,<2
xgboost>=2.0,<4
# pip-tools evaluates XGBoost's Linux/x86_64 NCCL marker on the resolver host.
# Keeping the marker as an explicit conditional input makes the same lock
# regenerate on macOS and GitHub's Ubuntu runner without installing CUDA code
# on other platforms.
nvidia-nccl-cu12==2.31.2 ; platform_system == "Linux" and platform_machine == "x86_64"
onnxmltools>=1.12,<2
onnxruntime>=1.17,<2
scapy>=2.5,<3
matplotlib>=3.7,<4
pyarrow>=14,<24
psutil>=5.9,<8
reportlab>=4,<5
