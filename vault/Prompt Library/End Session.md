# Prompt — end a session

```text
Create a unique dated handoff using Session Handoff Template. Reconcile the
task record, branch/HEAD/upstream, dirty and unpushed state, running workers,
tests, GitHub checks, protected state, exclusions, blockers, not-verified list,
and the exact first next action. Append a pointer to the task/hub. Keep volatile
facts out of AGENTS.md and CLAUDE.md.
```
